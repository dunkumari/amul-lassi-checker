# name: Amul Lassi Telegram Alert

# on:
#   schedule:
#     - cron: "*/30 * * * *"
#   workflow_dispatch:

# jobs:
#   check:
#     runs-on: ubuntu-latest

#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 18

#       - run: npm install
#       - run: node checkAmulLassi.js
#         env:
#           TG_BOT_TOKEN: ${{ secrets.TG_BOT_TOKEN }}
#           TG_CHAT_ID: ${{ secrets.TG_CHAT_ID }}
